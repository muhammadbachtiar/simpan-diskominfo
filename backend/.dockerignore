# Git
.git
.gitignore

# Environment files - .env is included for production builds
# .env is NOT ignored because we need it in the container
.env.local
.env.*.local

# Existing vendor (will be installed fresh in Docker)
vendor

# IDE
.idea
.vscode
*.swp
*.swo

# Node modules
node_modules

# Test files
tests
phpunit.xml
.phpunit.cache

# Docker entrypoint (created inside Dockerfile)
docker-entrypoint.sh

# Logs
*.log
logs/
storage/logs/*
!storage/logs/.gitignore
